# Backend — Qamar Abbas Portfolio API

Express + MongoDB + Socket.IO REST API. See the root `README.md` for full setup,
environment variables, API reference and deployment notes.

Quick start:

```bash
npm install
npm run seed   # creates the initial admin account from .env
npm run dev    # starts the API + Socket.IO server on PORT (default 5000)
```
